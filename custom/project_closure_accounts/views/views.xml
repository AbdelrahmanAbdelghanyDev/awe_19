<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="project_closure_accounts.list">
      <field name="name">ProjectClosureAccounts list</field>
      <field name="model">project_closure_accounts.project_closure_accounts</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="project_closure_accounts.action_window">
      <field name="name">ProjectClosureAccounts window</field>
      <field name="res_model">project_closure_accounts.project_closure_accounts</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="project_closure_accounts.action_server">
      <field name="name">ProjectClosureAccounts server</field>
      <field name="model_id" ref="model_project_closure_accounts_project_closure_accounts"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ProjectClosureAccounts" id="project_closure_accounts.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="project_closure_accounts.menu_1" parent="project_closure_accounts.menu_root"/>
    <menuitem name="Menu 2" id="project_closure_accounts.menu_2" parent="project_closure_accounts.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="project_closure_accounts.menu_1_list" parent="project_closure_accounts.menu_1"
              action="project_closure_accounts.action_window"/>
    <menuitem name="Server to list" id="project_closure_accounts" parent="project_closure_accounts.menu_2"
              action="project_closure_accounts.action_server"/>
    -->
  </data>
</odoo>