<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="working_schedule_shifts.list">
      <field name="name">WorkingScheduleShifts list</field>
      <field name="model">working_schedule_shifts.working_schedule_shifts</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="working_schedule_shifts.action_window">
      <field name="name">WorkingScheduleShifts window</field>
      <field name="res_model">working_schedule_shifts.working_schedule_shifts</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="working_schedule_shifts.action_server">
      <field name="name">WorkingScheduleShifts server</field>
      <field name="model_id" ref="model_working_schedule_shifts_working_schedule_shifts"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="WorkingScheduleShifts" id="working_schedule_shifts.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="working_schedule_shifts.menu_1" parent="working_schedule_shifts.menu_root"/>
    <menuitem name="Menu 2" id="working_schedule_shifts.menu_2" parent="working_schedule_shifts.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="working_schedule_shifts.menu_1_list" parent="working_schedule_shifts.menu_1"
              action="working_schedule_shifts.action_window"/>
    <menuitem name="Server to list" id="working_schedule_shifts" parent="working_schedule_shifts.menu_2"
              action="working_schedule_shifts.action_server"/>
    -->
  </data>
</odoo>